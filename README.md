# AP Wiki

AP Wiki is a first-pass Tauri desktop prototype for maintaining structured game knowledge.

## Commands

- `npm install`
- `npm run tauri:dev`
- `npm run build`
- `npm run publish`

Portable output is written to `Publish/APWikiDesktop.exe`.
